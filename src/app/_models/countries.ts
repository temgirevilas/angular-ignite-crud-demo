// import { city } from './city';

export class Countries {
    code2!: string;
    code!: string;
    name!: string;
    continent !: string;
    region!: string;
    population!: number;
    isDeleting: boolean = false;
}